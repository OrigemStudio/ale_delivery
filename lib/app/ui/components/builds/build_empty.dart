import '../../../../_exports.dart';

class BuildEmpty extends StatelessWidget {
  const BuildEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Não existem catalógos adicionados'),
    );
  }
}
